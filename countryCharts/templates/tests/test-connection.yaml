apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "countryCharts.fullname" . }}-test-connection"
  labels:
    {{- include "countryCharts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "countryCharts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
